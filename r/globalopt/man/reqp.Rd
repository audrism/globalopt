% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api2.R
\name{reqp}
\alias{reqp}
\title{Recursive quadratic programming (REQP)}
\usage{
reqp(
  x0,
  imax,
  constraints = NULL,
  n_eq = 0L,
  n_ineq = 0L,
  r1 = 1,
  scale = 0.25,
  delta = 1e-04,
  eps = 1e-04,
  objective = "furasn",
  trace = TRUE
)
}
\arguments{
\item{x0}{Starting point.}

\item{imax}{Maximum iterations.}

\item{constraints}{\code{NULL} for unconstrained, or a function \code{g(x)}
returning a numeric vector: first \code{n_eq} components are equalities
(feasible when 0), the remaining \code{n_ineq} are inequalities (feasible
when >= 0).}

\item{n_eq, n_ineq}{Number of equality/inequality constraint components
returned by \code{constraints}.}

\item{r1}{Penalty parameter.}

\item{scale}{Scaling parameter.}

\item{delta}{Finite-difference gradient step.}

\item{eps}{Tolerance level.}

\item{objective}{An R function taking a numeric vector and returning a
scalar, or the name of a compiled built-in objective
(see \code{\link[=builtin_objectives]{builtin_objectives()}}).}

\item{trace}{If \code{TRUE}, return all sampled points and values.}
}
\value{
An object of class \code{globalopt_result}: a list with elements
\code{best_x}, \code{best_f}, \code{evals}, \code{points} (matrix of evaluated points, or
\code{NULL} when \code{trace = FALSE}), \code{values}, \code{method}, \code{backend},
\code{best_iter}.
}
\description{
Constrained nonlinear minimization by recursive quadratic programming
with BFGS updates and finite-difference gradients (routine \code{REQP}).
}
\examples{
r <- reqp(c(0.2, 0.2), 50, objective = "furasn")
r$best_f
}
