% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api2.R
\name{anal1}
\alias{anal1}
\title{Selection of dominant variables and interactions (ANAL1)}
\usage{
anal1(
  a,
  b,
  points,
  values,
  harmonics = 7L,
  max_selected = 8L,
  interactions = TRUE
)
}
\arguments{
\item{a, b}{Numeric vectors of lower and upper bounds.}

\item{points}{Matrix (\code{m} rows, \code{n} columns) of sample points in
\verb{[a, b]}; \code{m} must be in \verb{10..300}.}

\item{values}{Numeric vector of the \code{m} objective values.}

\item{harmonics}{Number of harmonics investigated (1..7).}

\item{max_selected}{Maximum number of selected variables/interactions
(1..30).}

\item{interactions}{If \code{TRUE}, consider variable pairs as well as single
variables.}
}
\value{
A list with \code{influence} (sorted influence measures),
\code{variables} (matrix of 1-based variable indices; second column 0 for
single variables) and \code{status} (Fortran IFAIL code, 0 = OK).
}
\description{
Harmonic-analysis screening of variable influence from a sample of
points and objective values (routine \code{ANAL1}).  The routine does not
evaluate the objective itself: supply a design (for example the trace of
a \code{\link[=mig2]{mig2()}} run) and its values.
}
\examples{
r <- mig2(c(-1, -1), c(1, 1), 100, "furasn")
anal1(c(-1, -1), c(1, 1), r$points, r$values)
}
