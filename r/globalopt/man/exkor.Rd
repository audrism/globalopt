% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api2.R
\name{exkor}
\alias{exkor}
\title{Coordinate-wise Bayesian optimization (EXKOR)}
\usage{
exkor(
  a,
  b,
  evaluations,
  x0 = (a + b)/2,
  model_evals = 6L,
  cycles = 2L,
  first_coord = 1L,
  acc = 0.01,
  objective = "furasn",
  backend = c("fortran", "reference"),
  trace = TRUE
)
}
\arguments{
\item{a, b}{Numeric vectors of lower and upper bounds.}

\item{evaluations}{Maximum evaluations per one-dimensional search
(Fortran limit: 500).}

\item{x0}{Starting point.}

\item{model_evals}{Evaluations for model estimation in each
one-dimensional search (>= 6).}

\item{cycles}{Number of full coordinate cycles.}

\item{first_coord}{Index of the coordinate optimized first.}

\item{acc}{Requested accuracy (applied to the value and each coordinate).}

\item{objective}{An R function taking a numeric vector and returning a
scalar, or the name of a compiled built-in objective
(see \code{\link[=builtin_objectives]{builtin_objectives()}}).}

\item{backend}{\code{"fortran"} runs the original compiled Fortran routine;
\code{"reference"} runs the pure-R reference implementation.}

\item{trace}{If \code{TRUE}, return all sampled points and values.}
}
\value{
An object of class \code{globalopt_result}: a list with elements
\code{best_x}, \code{best_f}, \code{evals}, \code{points} (matrix of evaluated points, or
\code{NULL} when \code{trace = FALSE}), \code{values}, \code{method}, \code{backend},
\code{best_iter}.
}
\description{
Coordinate descent where each one-dimensional subproblem is solved by the
Wiener-model global method of \code{\link[=extr]{extr()}} (routine \code{EXKOR}).
}
\examples{
r <- exkor(c(-1, -1), c(1, 1), 50, x0 = c(0.1, 0.1), objective = "furasn")
r$best_f
}
