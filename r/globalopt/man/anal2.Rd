% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api2.R
\name{anal2}
\alias{anal2}
\title{Variable-influence analysis via covariance eigenstructure (ANAL2)}
\usage{
anal2(a, b, points, values)
}
\arguments{
\item{a, b}{Numeric vectors of lower and upper bounds.}

\item{points}{Matrix (\code{m} rows, \code{n} columns) of sample points in
\verb{[a, b]}; \code{m} must be in \verb{10..300}.}

\item{values}{Numeric vector of the \code{m} objective values.}
}
\value{
A list with \code{influence} (per-variable influence in the original
coordinates), \code{influence_eigen} (influence in the eigen coordinate
system), \code{eigenvectors} (n x n matrix) and \code{status} (IFAIL, 0 = OK).
}
\description{
Influence analysis of variables from a sample of points and objective
values via a factor/covariance decomposition (routine \code{ANAL2}).  Like
\code{\link[=anal1]{anal1()}}, the objective is not evaluated.
}
\examples{
r <- mig2(c(-1, -1), c(1, 1), 100, "furasn")
anal2(c(-1, -1), c(1, 1), r$points, r$values)
}
