% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{bayes1}
\alias{bayes1}
\title{One-step Bayesian global minimization (BAYES1)}
\usage{
bayes1(
  a,
  b,
  evaluations,
  initial_points,
  objective = "furasn",
  local_minimizer = NULL,
  backend = c("fortran", "reference"),
  trace = TRUE,
  seed = NULL
)
}
\arguments{
\item{a, b}{Numeric vectors of lower and upper bounds.}

\item{evaluations}{Total number of objective evaluations.}

\item{initial_points}{Number of initial LP-tau points (\verb{1 <= initial_points <= evaluations}).}

\item{objective}{An R function taking a numeric vector and returning a
scalar, or the name of a compiled built-in objective
(see \code{\link[=builtin_objectives]{builtin_objectives()}}).}

\item{local_minimizer}{Optional local refinement: an adapter name accepted
by \code{\link[=local_minimize]{local_minimize()}} or a function \code{f(objective, x0, lower, upper)}
returning \code{list(par, value)}.}

\item{backend}{\code{"fortran"} runs the original compiled Fortran routine;
\code{"reference"} runs the pure-R reference implementation.}

\item{trace}{If \code{TRUE}, return all sampled points and values.}

\item{seed}{Optional integer seed for the ATS random generator state.
\code{NULL} uses the canonical initial state of the original library
(deterministic, reproduces a fresh-process Fortran run).}
}
\value{
An object of class \code{globalopt_result}: a list with elements
\code{best_x}, \code{best_f}, \code{evals}, \code{points} (matrix of evaluated points, or
\code{NULL} when \code{trace = FALSE}), \code{values}, \code{method}, \code{backend},
\code{best_iter}.
}
\description{
The one-step Bayesian method of Mockus: \code{initial_points} evaluations at
LP-tau (Sobol-type) points followed by planned points chosen by
maximizing a one-step expected-improvement criterion over a simple
surrogate (routine \code{BAYES1} of the original library).
}
\details{
The Fortran backend enforces \code{length(a) <= 20} and
\code{evaluations <= 1000} (limits of the original working arrays).
}
\examples{
r <- bayes1(c(-1, -1), c(1, 1), 100, 20, "furasn")
r$best_f
}
